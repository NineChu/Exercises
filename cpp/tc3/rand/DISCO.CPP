#include <stdio.h>
#include <conio.h>
#include <dos.h>

void main()
{
    int o = 0
    clrscr();
    while(1=1)
    {
    textcolor(o);
    o++;
    for(int i = 1; i<2000; i++)
    {
	printf("M");
    }
    getch();
    }
}